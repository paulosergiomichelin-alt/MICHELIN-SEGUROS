rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Apenas usuários autenticados podem acessar qualquer documento
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
